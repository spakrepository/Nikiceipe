
export class Test {

    constructor() {
    }

    add(x: number, y: number): number {
        return x + y;
    }

    minus(x: number, y: number): number {
        return x - y;
    }

    multiplication(x: number, y: number): number {
        return x * y;
    }
}